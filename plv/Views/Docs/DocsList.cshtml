@model plv.ViewModels.DocumentListViewModel

@{
    ViewData["Title"] = "DocsList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <div class="plog col-md-6">
        <h1 class="display-4 text-center emboss">@Model.CurrentSection</h1>
        <div class="table-style">
            <table id="docs">
                <thead>
                    <tr>
                        <th>Nazwa</th>
                        <th>aaa</th>
                        <th>aaa</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var doc in Model.Documents)
                    {
                        <tr>
                            <td>
                                <a href="/Docs/Details/@doc.Id"> <p>@doc.FilePath</p></a>
                            </td>
                            <td>aaa</td>
                            <td>aaa</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>


            @if (this.User.IsInRole(Model.CurrentSection + "-upload") || this.User.IsInRole("Admin"))
            {
                <a href="/Docs/Create/@Model.CurrentSection"><p class="clikp">Add document</p></a>
            }
        </div>


        @section Scripts
{
            <script>
                $(document).ready(function () {
                    $('#docs').DataTable();
                });
            </script>
        }
